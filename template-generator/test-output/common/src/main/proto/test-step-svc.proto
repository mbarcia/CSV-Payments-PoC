syntax = "proto3";

option java_package = "test.package.grpc";

message TestInput {
  string name = 1;
  string createdAt = 2;
  int32 count = 3;
}

message TestOutput {
  string token = 4;
  string values = 5;
}

service ProcessService {
  rpc remoteProcess(TestInput) returns (TestOutput);
}